import 'package:flutter/material.dart';
class GuessTheCheckScreen extends StatelessWidget {
  const GuessTheCheckScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
