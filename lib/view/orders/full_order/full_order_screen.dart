import 'package:flutter/material.dart';
class FullOrderScreen extends StatelessWidget {
  const FullOrderScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
